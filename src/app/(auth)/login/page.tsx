export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center" style={{ backgroundColor: 'var(--color-cream)' }}>
      <div className="w-full max-w-sm rounded-xl p-8 shadow-lg" style={{ backgroundColor: 'var(--color-surface)' }}>
        <h1 className="mb-6 text-center text-2xl font-semibold" style={{ color: 'var(--color-text-primary)' }}>
          CurryDash Central Hub
        </h1>
        <p className="text-center text-sm" style={{ color: 'var(--color-text-muted)' }}>
          Authentication coming in Story 2.1
        </p>
      </div>
    </main>
  )
}
