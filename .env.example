# CurryDash Central Hub — Environment Variables Template
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local — it is gitignored.

# ─────────────────────────────────────────────
# Authentication (Auth.js / NextAuth)
# ─────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# GitHub OAuth App (for NextAuth GitHub provider)
# Create at: https://github.com/settings/developers
GITHUB_ID=
GITHUB_SECRET=

# Google OAuth 2.0 (for NextAuth Google provider)
# Create at: https://console.cloud.google.com → APIs & Services → Credentials
# Redirect URI: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=                     # format: xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=                 # format: GOCSPX-...

# ─────────────────────────────────────────────
# Supabase (Database + Auth + Realtime)
# ─────────────────────────────────────────────
# Find at: https://supabase.com/dashboard → Project → Settings → API
SUPABASE_URL=                         # format: https://xxx.supabase.co
SUPABASE_ANON_KEY=                    # format: eyJ... (safe for browser)
SUPABASE_SERVICE_ROLE_KEY=            # format: eyJ... (secret — server only)

# ─────────────────────────────────────────────
# Jira Integration
# ─────────────────────────────────────────────
# API token: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_BASE_URL=                        # format: https://xxx.atlassian.net
JIRA_EMAIL=                           # Atlassian account email
JIRA_API_TOKEN=                       # Atlassian API token
# Generate with: openssl rand -base64 32
JIRA_WEBHOOK_SECRET=

# ─────────────────────────────────────────────
# GitHub Integration
# ─────────────────────────────────────────────
# Personal Access Token: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user, user:email
GITHUB_TOKEN=                         # format: ghp_...
# Generate with: openssl rand -base64 32
GITHUB_WEBHOOK_SECRET=

# ─────────────────────────────────────────────
# AI Models
# ─────────────────────────────────────────────
ANTHROPIC_API_KEY=                    # format: sk-ant-...
GEMINI_API_KEY=                       # format: AIza... (from https://aistudio.google.com/apikey)
OPENAI_API_KEY=                       # format: sk-...

# ─────────────────────────────────────────────
# Vercel
# ─────────────────────────────────────────────
# API token: https://vercel.com/account/tokens
VERCEL_TOKEN=
# Generate with: openssl rand -base64 32
CRON_SECRET=
